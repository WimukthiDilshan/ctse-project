name: Deploy Student Service to Azure

on:
  push:
    branches:
      - main
    paths:
      - 'services/student-service/**'
      - '.github/workflows/deploy-student-service.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # SAST Scan Step (DevSecOps)
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_STUDENT }}
        with:
          args: >
            -Dsonar.projectKey=ctse-project_student-service
            -Dsonar.organization=${{ secrets.SONAR_ORG }}
            -Dsonar.sources=services/student-service

      # Docker Build and Push
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push container image to Docker Hub
        uses: docker/build-push-action@v3
        with:
          context: ./services/student-service
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/student-service:latest

      # Azure Login & Deploy
      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v1
        with:
          imageToDeploy: ${{ secrets.DOCKER_USERNAME }}/student-service:latest
          resourceGroup: ${{ secrets.RESOURCE_GROUP }}
          containerAppName: student-service
          containerAppEnvironment: ${{ secrets.CONTAINER_APP_ENV }}
          targetPort: 5001
          ingress: external
